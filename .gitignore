# Dependencies
node_modules/
# Note: package-lock.json SHOULD be committed (for consistent installs)
# yarn.lock and pnpm-lock.yaml are for alternative package managers
yarn.lock
pnpm-lock.yaml

# Build outputs
dist/
release/
build/
*.tsbuildinfo

# Built assets (do not commit)
dist/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
env.bak/
venv.bak/
.venv/
*.egg-info/
*.egg
pip-log.txt
pip-delete-this-directory.txt

# LEANN (installed/cloned repository - should be installed, not committed)
# Ignore the entire LEANN directory
python/LEANN/

# But keep our Python server files (these should be committed)
!python/server.py
!python/requirements.txt

# Python cache files
python/**/__pycache__/
python/**/*.pyc
python/**/*.pyo
python/**/*.egg-info/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Environment variables
.env
.env.local
.env.*.local
*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Electron
out/
app/
*.asar

# Temporary files
*.tmp
*.temp
.cache/

# Safety: ignore accidental binaries/scripts that should never be committed
agentic-ai-assistant@1.0.0
concurrently
npm
tsc
vite

# Do not commit the bundled reverse proxy source (local-only)
ai-reverse-proxy-main/

